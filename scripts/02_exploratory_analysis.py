# 1. Import libraries (pandas, matplotlib, os).
# 2. Load the cleaned data from 'data/cleaned_data.csv'.
# 3. Generate summary statistics and save them as a text file in the outputs folder.
# 4. Create visualizations:
#    - Histograms for each numeric column.
#    - A scatter plot for two selected numeric variables.
# 5. Save all plots to the 'outputs' folder.
# 6. Print a completion message.