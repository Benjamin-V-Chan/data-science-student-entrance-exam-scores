# 1. Import libraries (pandas, scikit-learn, os).
# 2. Load the cleaned data from 'data/cleaned_data.csv'.
# 3. Preprocess data:
#    - Separate features and target (assume the last column is the target).
#    - Apply one-hot encoding for categorical features.
# 4. Split the data into training and test sets.
# 5. Train a model (using Linear Regression as an example).
# 6. Evaluate model performance using R² score.
# 7. Save the performance metrics to a text file in the outputs folder.
# 8. Print a completion message.